package com.bsro.controller.district;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.stereotype.Controller;

@Controller
public class ViewDistrictController {
	private final Log logger = LogFactory.getLog(ViewDistrictController.class);
}
